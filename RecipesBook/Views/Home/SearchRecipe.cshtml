@model IEnumerable<RecipesBook.Models.Entities.Recipe>
@{
    ViewData["Title"] = "Recipes";
    IEnumerable<Category> categories = ViewData["Categories"] as IEnumerable<Category>;
}

@Html.ActionLink("Search category", "SearchCategory", new { controller = "Home" }, new { @class = "btn btn-primary" })
@using (Html.BeginForm("SearchRecipe", "Home", FormMethod.Post, new { @class = "form" }))
{
    <label for="search">Enter name:</label>
    @Html.TextBox("search", null, new { @class = "form-control p-3" })
    <div class="input-group my-3">
        <div class="input-group-prepend">
            <label for="categories" class="input-group-text">Select categories</label>
        </div>
        @Html.DropDownList("categories", new MultiSelectList(categories, "ID", "Name"), new { multiple = "multiple", @class = "form-control" })

    </div>
    <input type="submit" value="Search" class="btn btn-success" />
}
<h1>Recipes</h1>
@if (Model.Count() == 0)
{


    <h5 class="text-warning">Nothing is found.</h5>
}
else
{

    <div class="row">
        @foreach (var item in Model)
        {
            <div id="recipe-@item.Id" class="card col-3 p-3">


                <img class="card-img-top" height="150" width="150">
                <div class="card-body">
                    <h5 class="card-title">@item.Name</h5>
                    <p class="card-text">@item.Description</p>
                    <a href="/recipes/@item.Id" class="btn btn-primary">View recipe</a>
                </div>
            </div>
        }
    </div>
    <nav class="nav-pagination">
        <ul class="pagination justify-content-center">
            <li class="page-item disabled">
                <a class="page-link" href="#" tabindex="-1" aria-disabled="true">Previous</a>
            </li>
            @for (int i = 0; i < Model.Count(); i++)
            {
                <li class="page-item"><a class="page-link" href="?page=@(i+1)">@(i+1)</a></li>
            }


            <li class="page-item">
                <a class="page-link" href="#">Next</a>
            </li>
        </ul>
    </nav>

}
@section scripts{


    <script>
        $(document).ready(function () {
            $("#categories").select2({
                placeholder: {
                    id: '', // the value of the option
                    text: 'None Selected'
                }, multiple: true
            });
        });
    </script>
}
